# See src/config.rs for all of the possible options.
# the commented-out lines are examples of values you could set, not the defaults.

bind = "0.0.0.0:28019"
api = false

[ui]
# engine_list_separator = true
# show_version_info = true
# stylesheet_url = "/themes/catppuccin-mocha.css"

[image_search]
# enabled = true

[engines]
# numbat = false
# fend = true

[urls.replace]
# "www.reddit.com" = "old.reddit.com"
# "medium.com" = "scribe.rip"
# ".medium.com" = "scribe.rip"

[urls.weight]
# (these are checked after replacing)
# setting the weight to 0 completely hides the result
# "quora.com" = 0.1
# ".quora.com" = 0.1
